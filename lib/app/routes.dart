import 'package:travelly/core/constants/route_constants.dart';

/// Named route definitions.
///
/// When adding named route navigation, register routes here
/// and reference via [RouteConstants].
class AppRoutes {
  AppRoutes._();

  // Route map will be populated when named routing is adopted.
  // Example:
  // static Map<String, WidgetBuilder> get routes => {
  //   RouteConstants.dashboard: (context) => const DashboardScreen(),
  //   RouteConstants.payments: (context) => const PaymentsScreen(),
  //   RouteConstants.documents: (context) => const DocumentsScreen(),
  // };
}
