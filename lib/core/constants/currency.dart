/// Global currency configuration for the app.
/// MOCK: Currently hardcoded but structured to be replaced by backend trip settings.
class AppCurrency {
  static String symbol = '₹';
  static String code = 'INR';
}
